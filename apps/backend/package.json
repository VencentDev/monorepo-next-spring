{
  "name": "@app/backend",
  "private": true,
  "scripts": {
    "dev": "set -a && [ ! -f .env ] || . ./.env && set +a && ./mvnw spring-boot:run",
    "build": "./mvnw clean package -DskipTests",
    "test": "./mvnw test",
    "lint": "./mvnw spotless:check",
    "format": "./mvnw spotless:apply"
  }
}
