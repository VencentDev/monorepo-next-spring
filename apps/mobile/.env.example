API_BASE_URL=http://10.0.2.2:8080
# Use the Web OAuth client ID as google_sign_in's serverClientId. Android also
# needs an Android OAuth client in the same Google Cloud project with package
# com.app.app_mobile and your debug/release signing SHA-1.
GOOGLE_CLIENT_ID=WEB_CLIENT_ID.apps.googleusercontent.com
